#include "main.h"
/**
 * print_id - Entry point.
 * Description: Print an integer.
 * @id: int to print.
 * Return: Numbers of printed characters.
 */
int print_id(int id)
{
	return (intochar(id));
}
