#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>

int _printf(const char *format, ...);
int _strlen(const char *s);
int _putchar(char c);
int _print_c(char c);
int _print_s(char *s);

#endif
